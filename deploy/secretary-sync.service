[Unit]
Description=OpenCouncil secretary — incremental sync (oneshot, driven by timer)
After=network.target surrealdb.service
Wants=surrealdb.service

[Service]
Type=oneshot
User=secretary
WorkingDirectory=/opt/secretary
EnvironmentFile=/opt/secretary/.env
ExecStart=/opt/secretary/.venv/bin/secretary reconcile
